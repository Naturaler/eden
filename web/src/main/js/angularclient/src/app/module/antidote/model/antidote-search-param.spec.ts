import { AntidoteSearchParam } from './antidote-search-param';

describe('AntidoteSearchParam', () => {
  it('should create an instance', () => {
    expect(new AntidoteSearchParam()).toBeTruthy();
  });
});
